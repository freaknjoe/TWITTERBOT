services:
  web:
    type: web
    name: cryptosocialbot
    env: python
    region: 'aws'
    plan: 'free'
    branch: main
    healthcheckPath: /
    startCommand: gunicorn app:app --bind 0.0.0.0:3000 --workers 1