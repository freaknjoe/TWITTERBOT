services:
  web:
    type: web
    name: cryptosocialbot
    env: python
    region: 'aws'
    plan: 'free'
    branch: main
    healthcheckPath: /
    startCommand: gunicorn flask_app:app --bind 0.0.0.0:8080 --workers 1